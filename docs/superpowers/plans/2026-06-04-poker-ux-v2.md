# 德州扑克 UX V2 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 7 项 UX 升级：动态座位、加注输入框、按钮立体感+音效、行动徽章、胜者动画+结算简化（3s自动）、结算展示公共牌、弃牌→随时观战+坐下。

**Architecture:** 纯前端改动集中在 `GameRoom.jsx`（当前 1185 行，本次会增长）；音效抽为独立模块 `src/helpers/sounds.js`；后端只在 Task 4（lastAction 广播）、Task 5（3s timeout）、Task 7（foldToSpectate 事件）有最小改动。

**Tech Stack:** React 19, Socket.IO, Web Audio API (无外部音频文件), CSS keyframe animations

---

## 文件映射

| 文件 | 涉及任务 |
|------|---------|
| `src/pages/GameRoom.jsx` | Task 1, 2, 3, 4b, 5, 6, 7 |
| `src/helpers/sounds.js` | Task 3（新建） |
| `server/sockets/socketHandlers.js` | Task 4a, 5, 7 |

---

## Task 1：动态座位位置

**Files:**
- Modify: `src/pages/GameRoom.jsx` — 删除 `SEAT_POS` 数组，修改 `PokerTable` 组件

- [ ] **Step 1：删除硬编码数组，添加动态计算函数**

在 `GameRoom.jsx` 中找到 `const SEAT_POS = [...]`（约第 46 行），将整个数组替换为：

```javascript
function getSeatPositions(n) {
  // i=0 = me (bottom center), others clockwise from top
  const cx = 50, cy = 42, rx = 42, ry = 24;
  return Array.from({ length: n }, (_, i) => {
    const angle = Math.PI / 2 + (2 * Math.PI * i / n);
    return {
      x: Math.max(8, Math.min(92, Math.round(cx + rx * Math.cos(angle)))),
      y: Math.max(16, Math.min(68, Math.round(cy + ry * Math.sin(angle)))),
    };
  });
}
```

- [ ] **Step 2：修改 `PokerTable` 组件使用动态位置**

找到 `PokerTable` 函数（约第 648 行），将：
```javascript
const pos = SEAT_POS[seatPos] || { x: 50, y: 50 };
```
改为：
```javascript
const seats = getSeatPositions(orderedPlayers.length);
```
并在 map 内改为：
```javascript
const pos = seats[seatPos] || { x: 50, y: 50 };
```

- [ ] **Step 3：目视验证**

启动前端（`npm start`），用 2/3/4 人测试座位分布：
- 2人：我底部(50,66)，对手顶部(50,18)
- 3人：我底部，左上(15,30)，右上(85,30)
- 4人：我底部，左(8,42)，顶(50,18)，右(92,42)

- [ ] **Step 4：提交**

```bash
cd D:\Claude\texas-poker
git add src/pages/GameRoom.jsx
git commit -m "feat: dynamic seat positions based on player count"
```

---

## Task 2：加注 UI 改造（输入框 + 置灰其他按钮）

**Files:**
- Modify: `src/pages/GameRoom.jsx` — 加注面板区域、行动按钮区域

- [ ] **Step 1：加注面板添加数字输入框**

找到加注面板 div（约第 524 行，`{showRaise && showActionButtons && canRaise && (`），在 slider 行的 `<div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>` 内，在 `<input type="range" .../>` **之后**、½POT 按钮**之前**添加：

```jsx
              <input
                type="number"
                min={minRaise} max={maxRaise}
                value={actualRaise}
                onChange={e => {
                  const v = Number(e.target.value);
                  if (!isNaN(v)) setRaiseAmount(v);
                }}
                style={{
                  width: 64, background: 'rgba(255,255,255,0.08)',
                  border: `1px solid ${(actualRaise < minRaise || actualRaise > maxRaise) ? '#ef4444' : 'rgba(255,255,255,0.2)'}`,
                  borderRadius: 8, color: '#f0d060', fontWeight: 700,
                  fontSize: 13, padding: '4px 6px', textAlign: 'center',
                  outline: 'none',
                }}
              />
```

- [ ] **Step 2：加注模式下置灰弃牌/跟注按钮**

找到弃牌按钮（约第 560 行），在其 `style` 的 `boxShadow` 后追加：
```javascript
opacity: showRaise ? 0.35 : 1,
pointerEvents: showRaise ? 'none' : 'auto',
```

找到跟注/过牌按钮（约第 566 行），同样追加：
```javascript
opacity: showRaise ? 0.35 : 1,
pointerEvents: showRaise ? 'none' : 'auto',
```

- [ ] **Step 3：添加「取消」按钮**

找到加注面板 div 内的顶部 `<div style={{ display: 'flex', ...justifyContent: 'space-between'... }}>` 中的右侧 `<span>`（显示金额的那行），在其后添加：

```jsx
              <button
                onClick={() => setShowRaise(false)}
                style={{
                  background: 'none', border: '1px solid rgba(255,255,255,0.2)',
                  borderRadius: 8, color: 'rgba(255,255,255,0.55)', fontSize: 11,
                  padding: '3px 10px', cursor: 'pointer',
                }}
              >取消</button>
```

- [ ] **Step 4：提交**

```bash
git add src/pages/GameRoom.jsx
git commit -m "feat: raise UI — number input, disable fold/call during raise, cancel button"
```

---

## Task 3：按钮立体感 + 点击音效模块

**Files:**
- Create: `src/helpers/sounds.js`
- Modify: `src/pages/GameRoom.jsx` — `actionBtn` 样式、`sendAction` 函数

- [ ] **Step 1：创建 `src/helpers/sounds.js`**

```javascript
let _ctx = null;
function ctx() {
  if (!_ctx) _ctx = new (window.AudioContext || window.webkitAudioContext)();
  return _ctx;
}

function tone(freq, dur, vol = 0.15, type = 'sine') {
  try {
    const c = ctx();
    const osc = c.createOscillator();
    const gain = c.createGain();
    osc.connect(gain); gain.connect(c.destination);
    osc.frequency.value = freq; osc.type = type;
    gain.gain.setValueAtTime(vol, c.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, c.currentTime + dur);
    osc.start(c.currentTime); osc.stop(c.currentTime + dur);
  } catch {}
}

export function playActionSound(action) {
  if (action === 'fold')  { tone(180, 0.15, 0.13); return; }
  if (action === 'check') { tone(440, 0.10, 0.12); return; }
  if (action === 'call')  { tone(440, 0.12, 0.15); return; }
  if (action === 'raise') {
    tone(440, 0.10, 0.15);
    setTimeout(() => tone(660, 0.15, 0.18), 80);
    return;
  }
  if (action === 'allin') {
    tone(440, 0.08, 0.15);
    setTimeout(() => tone(660, 0.08, 0.18), 60);
    setTimeout(() => tone(880, 0.20, 0.20), 120);
  }
}
```

- [ ] **Step 2：在 `GameRoom.jsx` 顶部导入**

在 `import Card from '../components/Card';` 行后添加：
```javascript
import { playActionSound } from '../helpers/sounds';
```

- [ ] **Step 3：在 `sendAction` 中播放音效**

找到 `sendAction` 函数（约第 329 行）：
```javascript
const sendAction = (action, amount = 0) => {
  setError(''); setShowRaise(false);
  socket.emit('playerAction', { roomId, action, amount });
};
```

改为：
```javascript
const sendAction = (action, amount = 0) => {
  setError(''); setShowRaise(false);
  playActionSound(action);
  socket.emit('playerAction', { roomId, action, amount });
};
```

- [ ] **Step 4：更新 `actionBtn` 样式对象（约第 636 行）**

找到：
```javascript
const actionBtn = {
  flex: 1, height: 56, borderRadius: 14, border: 'none', cursor: 'pointer',
  color: '#fff', fontWeight: 700, fontSize: 16, transition: 'opacity 0.15s',
};
```

替换为：
```javascript
const actionBtn = {
  flex: 1, height: 56, borderRadius: 14, border: 'none', cursor: 'pointer',
  color: '#fff', fontWeight: 700, fontSize: 16,
  transition: 'opacity 0.15s, transform 0.08s, box-shadow 0.08s',
  position: 'relative', top: 0,
};
```

然后在 `GameRoom.jsx` 的 `<style>` 标签（约第 634 行）内，在现有 keyframes 后追加：
```css
          .action-btn:active {
            transform: translateY(3px) !important;
            filter: brightness(0.88) !important;
          }
```

在三个操作按钮（弃牌、跟注、加注）各自的 JSX 上添加 `className="action-btn"`，并将各自的 `boxShadow` 改为带底部深色厚度的版本：

弃牌按钮 boxShadow：
```javascript
boxShadow: showRaise ? '0 4px 14px rgba(127,29,29,0.3)' : '0 6px 0 #5a0f0f, 0 8px 16px rgba(127,29,29,0.5)',
```

跟注按钮 boxShadow：
```javascript
boxShadow: showRaise ? '0 4px 14px rgba(20,83,45,0.3)' : '0 6px 0 #0a3018, 0 8px 16px rgba(20,83,45,0.5)',
```

加注按钮 boxShadow（非确认态）：
```javascript
boxShadow: showRaise ? '0 6px 0 #0f1e50, 0 8px 16px rgba(37,99,235,0.7)' : '0 6px 0 #0a1a5e, 0 8px 16px rgba(30,58,138,0.45)',
```

- [ ] **Step 5：提交**

```bash
git add src/helpers/sounds.js src/pages/GameRoom.jsx
git commit -m "feat: 3D action buttons and Web Audio click sounds"
```

---

## Task 4：行动徽章（lastAction 广播）

**Files:**
- Modify: `server/sockets/socketHandlers.js` — `broadcastToEach`, `processAfterAction`, `playerAction`, timer callback
- Modify: `src/pages/GameRoom.jsx` — `onUpdate` handler, `AvatarTimer`

### 4a：后端 lastAction

- [ ] **Step 1：修改 `broadcastToEach` 支持额外字段**

找到 `broadcastToEach` 函数（约第 480 行）：
```javascript
function broadcastToEach(io, room, event) {
  for (const player of room.players) {
    io.to(player.socketId).emit(event, { room: sanitizeRoom(room, player.socketId) });
  }
}
```

替换为：
```javascript
function broadcastToEach(io, room, event, extra = {}) {
  for (const player of room.players) {
    io.to(player.socketId).emit(event, { room: sanitizeRoom(room, player.socketId), ...extra });
  }
}
```

- [ ] **Step 2：修改 `processAfterAction` 接受并传递 lastAction**

找到（约第 178 行）：
```javascript
function processAfterAction(room, roomId) {
  if (!shouldAdvanceStreet(room)) {
    advanceTurn(room);
    broadcastToEach(io, room, 'gameStateUpdate');
    startNextPlayerTimer(room);
    return;
  }

  const active = room.players.filter(p => !p.folded);
  if (active.length <= 1) {
    resolveShowdown(room, io, room.roomId);
    return;
  }

  const canAct = active.filter(p => p.chips > 0);
  if (canAct.length === 0) room.currentTurnIndex = -1;
  broadcastToEach(io, room, 'gameStateUpdate');
  advanceStreet(roomId);
}
```

替换为：
```javascript
function processAfterAction(room, roomId, lastAction = null) {
  const extra = lastAction ? { lastAction } : {};
  if (!shouldAdvanceStreet(room)) {
    advanceTurn(room);
    broadcastToEach(io, room, 'gameStateUpdate', extra);
    startNextPlayerTimer(room);
    return;
  }

  const active = room.players.filter(p => !p.folded);
  if (active.length <= 1) {
    resolveShowdown(room, io, room.roomId);
    return;
  }

  const canAct = active.filter(p => p.chips > 0);
  if (canAct.length === 0) room.currentTurnIndex = -1;
  broadcastToEach(io, room, 'gameStateUpdate', extra);
  advanceStreet(roomId);
}
```

- [ ] **Step 3：在 `playerAction` 事件中构造 lastAction 并传递**

找到（约第 60 行）：
```javascript
  socket.on('playerAction', ({ roomId, action, amount }) => {
    const room = roomManager.getRoom(roomId);
    if (!room) return;
    const actor = room.players[room.currentTurnIndex];
    if (!actor || actor.socketId !== socket.id) return;

    timerManager.clearTimer(socket.id);

    const valid = applyAction(room, actor, action, Number(amount) || 0);
    if (!valid) { socket.emit('actionError', { code: 'INVALID_ACTION' }); return; }

    processAfterAction(room, roomId);
  });
```

替换为：
```javascript
  socket.on('playerAction', ({ roomId, action, amount }) => {
    const room = roomManager.getRoom(roomId);
    if (!room) return;
    const actor = room.players[room.currentTurnIndex];
    if (!actor || actor.socketId !== socket.id) return;

    timerManager.clearTimer(socket.id);

    const valid = applyAction(room, actor, action, Number(amount) || 0);
    if (!valid) { socket.emit('actionError', { code: 'INVALID_ACTION' }); return; }

    const lastAction = { socketId: actor.socketId, action, amount: Number(amount) || 0 };
    processAfterAction(room, roomId, lastAction);
  });
```

- [ ] **Step 4：在超时回调中也传递 lastAction**

找到 `startNextPlayerTimer` 内的超时回调（约第 325 行）：
```javascript
    const onTimeout = (socketId, roomId) => {
      ...
      applyAction(r, timedOutActor, autoAction, 0);
      io.to(roomId).emit('timedOut', { socketId, autoAction });
      processAfterAction(r, roomId);
    };
```

（实际代码在 timerManager.startTimer 回调里）找到 `processAfterAction(r, roomId)` 调用，改为：
```javascript
      const lastAction = { socketId, action: autoAction, amount: 0 };
      processAfterAction(r, roomId, lastAction);
```

### 4b：前端行动徽章

- [ ] **Step 5：在 `GameRoom` 添加 `actionBadges` state 和处理逻辑**

在 `const [tauntBubbles, setTauntBubbles] = useState({});` 行后添加：
```javascript
  const [actionBadges, setActionBadges] = useState({});
```

找到 `onUpdate` handler（约第 201 行）：
```javascript
    const onUpdate = (payload) => {
      const r = payload?.room ?? payload;
      setRoom(r); setError(''); setShowRaise(false);
      ...
    };
```

在 `setRoom(r);` 行后添加：
```javascript
      if (payload.lastAction) {
        const { socketId, action, amount } = payload.lastAction;
        if (socketId !== socket.id) playActionSound(action); // 他人行动也播音效
        const key = Date.now();
        setActionBadges(prev => ({ ...prev, [socketId]: { action, amount, key } }));
        setTimeout(() => {
          setActionBadges(prev => {
            const next = { ...prev };
            if (next[socketId]?.key === key) delete next[socketId];
            return next;
          });
        }, 1500);
      }
```

- [ ] **Step 6：将 `actionBadges` 传入 `PokerTable`，再传给 `AvatarTimer`**

找到 `<PokerTable` 调用（约第 427 行），添加 prop：
```jsx
              actionBadges={actionBadges}
```

找到 `PokerTable` 函数签名（约第 648 行），在解构参数里加 `actionBadges`，并在渲染 `AvatarTimer` 时传：
```jsx
            actionBadge={actionBadges?.[player.socketId]}
```

- [ ] **Step 7：在 `AvatarTimer` 中渲染行动徽章**

在 `AvatarTimer` 函数签名（约第 690 行）的参数里加 `actionBadge`。

在 avatar circle div 内（`<div style={{ position: 'relative', width: sz, height: sz }}>`），在 SpeechBubble 渲染行后加：
```jsx
          {actionBadge && !bubble && <ActionBadge badge={actionBadge} />}
```

在文件末尾（`TauntPicker` 函数后）添加新组件：
```jsx
// ── 行动徽章（弃牌/跟注/过牌/加注/all-in）──────────────────
const BADGE_STYLES = {
  fold:  { bg: 'rgba(239,68,68,0.92)',   label: '弃牌' },
  call:  { bg: 'rgba(34,197,94,0.92)',   label: (a) => `跟注 ${a.amount}` },
  check: { bg: 'rgba(107,114,128,0.92)', label: '过牌' },
  raise: { bg: 'rgba(59,130,246,0.92)',  label: (a) => `加注 ${a.amount}` },
  allin: { bg: 'rgba(240,208,96,0.95)',  label: 'ALL IN', color: '#000' },
};

function ActionBadge({ badge }) {
  const s = BADGE_STYLES[badge.action];
  if (!s) return null;
  const label = typeof s.label === 'function' ? s.label(badge) : s.label;
  return (
    <div style={{
      position: 'absolute', bottom: 'calc(100% + 8px)', left: '50%',
      transform: 'translateX(-50%)',
      background: s.bg, borderRadius: 10, padding: '3px 8px',
      fontSize: 11, fontWeight: 800, color: s.color || '#fff',
      whiteSpace: 'nowrap', zIndex: 55, pointerEvents: 'none',
      animation: 'taunt-pop 0.2s cubic-bezier(0.34,1.56,0.64,1) forwards, taunt-fade 1.5s linear forwards',
    }}>{label}</div>
  );
}
```

- [ ] **Step 8：提交**

```bash
git add server/sockets/socketHandlers.js src/pages/GameRoom.jsx
git commit -m "feat: action badges and sounds on lastAction broadcast"
```

---

## Task 5：胜者动画 + 结算简化（后端 3s + WinAnimation）

**Files:**
- Modify: `server/sockets/socketHandlers.js` — 60s→3s timeout
- Modify: `src/pages/GameRoom.jsx` — winAnimating state, WinAnimation component, SettlementScreen 调用

### 5a：后端 3s 结算超时

- [ ] **Step 1：修改 `resolveShowdown` 中的超时逻辑**

找到（约第 459 行）：
```javascript
    // 60-second timeout: auto-spectate pending players
    room._settlementTimeout = setTimeout(() => {
      const r = roomManager.getRoom(roomId);
      if (!r || r.phase !== 'settlement') return;
      r.players.forEach(p => {
        if (p.readyStatus === 'pending') p.readyStatus = 'spectating';
      });
      const readyCount = r.players.filter(
        p => p.readyStatus === 'ready' || p.readyStatus === 'queued'
      ).length;
      if (readyCount >= 2) {
        startNextHand(roomId);
      } else {
        broadcastToEach(io, r, 'gameStateUpdate');
      }
    }, 60000);
```

替换为：
```javascript
    // 3-second auto-start: no manual ready needed
    room._settlementTimeout = setTimeout(() => {
      const r = roomManager.getRoom(roomId);
      if (!r || r.phase !== 'settlement') return;
      startNextHand(roomId);
    }, 3000);
```

同时，`settlementDeadline` 已在上面代码中定义为 `Date.now() + 60000`（约第 437 行），改为：
```javascript
    const settlementDeadline = Date.now() + 3000;
```

### 5b：前端胜者动画

- [ ] **Step 2：添加 `winAnimating` state 和 keyframes**

在 `const [settlementData, setSettlementData] = useState(null);` 行后添加：
```javascript
  const [winAnimating, setWinAnimating] = useState(false);
```

在 `<style>` 标签（约第 634 行）内追加 keyframes：
```css
          @keyframes win-fade-in {
            from { opacity:0; transform:scale(0.85); }
            to   { opacity:1; transform:scale(1); }
          }
          @keyframes crown-bounce {
            from { transform:translateY(0) scale(1); }
            to   { transform:translateY(-10px) scale(1.12); }
          }
```

- [ ] **Step 3：修改 `onShowdown` 触发 winAnimating**

找到 `onShowdown` handler（约第 219 行）：
```javascript
    const onShowdown = ({ room: r, results, wasMuckWin, settlementDeadline: deadline }) => {
      setRoom(r);
      setSettlementData({ results: results || [], wasMuckWin });
      setSettlementDeadline(deadline);
      setCardReveals({});
      const me2 = r.players.find(p => p.socketId === socket.id);
      setMyReadyStatus(me2?.readyStatus || 'pending');
      const winners = (results || []).filter(x => x.delta > 0).map(x => x.nickname);
      setMessage(winners.length ? `🏆 ${winners.join('、')} 获胜！` : '');
    };
```

替换为：
```javascript
    const onShowdown = ({ room: r, results, wasMuckWin, settlementDeadline: deadline }) => {
      setRoom(r);
      setSettlementData({ results: results || [], wasMuckWin });
      setSettlementDeadline(deadline);
      setCardReveals({});
      setMessage('');
      // Show win animation for 1s before settlement panel
      setWinAnimating(true);
      setTimeout(() => setWinAnimating(false), 1000);
    };
```

- [ ] **Step 4：添加 `WinAnimation` 组件（文件末尾）**

```jsx
// ── 胜者动画覆盖层（1s）──────────────────────────────────────
function WinAnimation({ results }) {
  const winners = (results || []).filter(r => r.delta > 0);
  if (winners.length === 0) return null;
  const text = winners.length === 1
    ? `🏆 ${winners[0].nickname} 赢了！`
    : `🏆 ${winners.map(w => w.nickname).join(' & ')} 平分！`;
  return (
    <div style={{
      position: 'absolute', inset: 0, zIndex: 45,
      background: 'rgba(0,0,0,0.78)',
      display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
      animation: 'win-fade-in 0.25s ease forwards',
    }}>
      <div style={{
        fontSize: 72, lineHeight: 1,
        animation: 'crown-bounce 0.4s ease-in-out infinite alternate',
      }}>👑</div>
      <div style={{
        color: '#f0d060', fontSize: 26, fontWeight: 800, marginTop: 14,
        textShadow: '0 2px 24px rgba(240,208,96,0.9)',
        textAlign: 'center', padding: '0 24px',
      }}>{text}</div>
    </div>
  );
}
```

- [ ] **Step 5：在 GameRoom render 里挂载 WinAnimation，条件显示 SettlementScreen**

找到（约第 664 行）：
```jsx
        {(room.phase === 'settlement' || settlementData) && (
          <SettlementScreen
            ...
          />
        )}
```

替换为：
```jsx
        {winAnimating && settlementData && (
          <WinAnimation results={settlementData.results} />
        )}

        {(room.phase === 'settlement' || settlementData) && !winAnimating && (
          <SettlementScreen
            settlementData={settlementData}
            room={room}
            mySocketId={mySocketId}
            settlementCountdown={settlementCountdown}
            cardReveals={cardReveals}
            onRevealCards={sendRevealCards}
          />
        )}
```

- [ ] **Step 6：提交**

```bash
git add server/sockets/socketHandlers.js src/pages/GameRoom.jsx
git commit -m "feat: 1s win animation, 3s auto-settlement, remove manual ready flow"
```

---

## Task 6：结算面板重设计（公共牌 + 亮牌显示牌型 + 移除准备按钮）

**Files:**
- Modify: `src/pages/GameRoom.jsx` — `SettlementScreen` 组件

- [ ] **Step 1：重写 `SettlementScreen` 组件**

找到 `function SettlementScreen({...})` 定义（约第 887 行）。将整个函数替换为：

```jsx
function SettlementScreen({ settlementData, room, mySocketId, settlementCountdown, cardReveals, onRevealCards }) {
  const { results = [] } = settlementData || {};
  const hasRevealed = !!cardReveals[mySocketId];

  return (
    <div style={{
      position: 'absolute', inset: 0, zIndex: 40, background: 'rgba(0,0,0,0.8)',
      display: 'flex', alignItems: 'center', justifyContent: 'center',
      padding: '52px 14px 14px',
    }}>
      <div style={{
        background: '#1a2f4a', borderRadius: 20, padding: 16,
        border: '1px solid rgba(240,208,96,0.28)', width: '100%',
        maxHeight: '100%', overflow: 'auto',
        display: 'flex', flexDirection: 'column', gap: 11,
      }}>
        <h2 style={{ color: '#f0d060', fontWeight: 700, fontSize: 20, textAlign: 'center', margin: 0 }}>🃏 本局结算</h2>

        {/* 公共牌 */}
        {room.communityCards?.length > 0 && (
          <div style={{ display: 'flex', gap: 5, justifyContent: 'center' }}>
            {room.communityCards.map((card, i) => (
              <Card key={i} card={card} size="sm" />
            ))}
          </div>
        )}

        {/* 结果列表 */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 7 }}>
          {results.map(r => {
            const revealedCards = cardReveals[r.socketId] || r.holeCards || [];
            const cardsVisible = revealedCards.length > 0 && revealedCards.every(c => c !== 'hidden');
            const isWinner = r.delta > 0;
            const player = room.players.find(p => p.socketId === r.socketId);
            return (
              <div key={r.socketId} style={{
                borderRadius: 14, padding: '10px 12px',
                display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 8,
                background: isWinner ? 'rgba(34,197,94,0.18)' : 'rgba(255,255,255,0.08)',
                border: `1px solid ${isWinner ? 'rgba(34,197,94,0.38)' : 'rgba(255,255,255,0.14)'}`,
              }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: 8, minWidth: 0 }}>
                  <span style={{ fontSize: 20, flexShrink: 0 }}>
                    {isWinner ? '👑' : AVATARS[(player?.seatIndex || 0) % AVATARS.length]}
                  </span>
                  <div style={{ minWidth: 0 }}>
                    <div style={{ fontSize: 13, fontWeight: 600, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                      {r.nickname}{r.socketId === mySocketId ? ' (我)' : ''}
                    </div>
                    {cardsVisible && r.handName && (
                      <div style={{ fontSize: 11, color: '#f0d060', fontWeight: 600 }}>
                        {HAND_NAME_MAP[r.handName] || r.handName}
                      </div>
                    )}
                  </div>
                </div>
                <div style={{ display: 'flex', alignItems: 'center', gap: 7, flexShrink: 0 }}>
                  <div style={{ display: 'flex', gap: 3 }}>
                    {revealedCards.map((card, i) =>
                      card === 'hidden'
                        ? <div key={i} style={{ width: 26, height: 38, background: '#1e3a6e', border: '1px solid rgba(59,130,246,0.35)', borderRadius: 4, display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'rgba(255,255,255,0.28)', fontSize: 13 }}>?</div>
                        : <Card key={i} card={card} size="sm" />
                    )}
                  </div>
                  <span style={{ fontWeight: 700, fontSize: 14, width: 46, textAlign: 'right', color: isWinner ? '#4ade80' : '#f87171' }}>
                    {r.delta > 0 ? `+${r.delta}` : r.delta}
                  </span>
                </div>
              </div>
            );
          })}
        </div>

        {/* 亮牌按钮 */}
        {!hasRevealed
          ? <button onClick={onRevealCards} style={{ width: '100%', padding: '10px 0', borderRadius: 14, border: '1px solid rgba(240,208,96,0.38)', background: 'none', color: '#f0d060', fontWeight: 600, fontSize: 14, cursor: 'pointer' }}>亮牌 🂠</button>
          : <div style={{ textAlign: 'center', color: 'rgba(255,255,255,0.35)', fontSize: 12 }}>已亮牌</div>
        }

        {/* 3s 倒计时进度条 */}
        {settlementCountdown > 0 && (
          <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
            <div style={{ height: 3, background: 'rgba(0,0,0,0.3)', borderRadius: 4, overflow: 'hidden' }}>
              <div style={{ height: '100%', background: '#f0d060', transition: 'width 1s linear', width: `${(settlementCountdown / 3) * 100}%` }} />
            </div>
            <p style={{ color: 'rgba(255,255,255,0.28)', fontSize: 11, textAlign: 'center', margin: 0 }}>{settlementCountdown}s 后自动开始下一局</p>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2：清理 GameRoom 中不再使用的变量和函数**

找到并删除（或注释）以下不再使用的代码：
- `const [myReadyStatus, setMyReadyStatus] = useState(...)` → 删除
- `const sendReady = () => { ... }` → 删除
- `const sendSpectate = () => { ... }` → 删除（`sendQueueNextHand` 保留，Task 7 会用）
- `onShowdown` 中的 `setMyReadyStatus(...)` → 已在 Task 5 中删除
- `onStarted` 中的 `setMyReadyStatus(...)` → 删除
- `SettlementScreen` 调用处已在 Task 5 移除了 `myReadyStatus`、`onReady`、`onSpectate`、`onQueueNextHand` props

- [ ] **Step 3：提交**

```bash
git add src/pages/GameRoom.jsx
git commit -m "feat: settlement redesign — community cards, hand name on reveal, auto-close 3s"
```

---

## Task 7：弃牌→随时观战 + 坐下按钮

**Files:**
- Modify: `server/sockets/socketHandlers.js` — 新增 `foldToSpectate` 事件
- Modify: `src/pages/GameRoom.jsx` — 行动区新增两个按钮

### 7a：后端 foldToSpectate

- [ ] **Step 1：在 socketHandlers.js 添加 foldToSpectate 事件**

在 `socket.on('queueForNextHand', ...)` 块（约第 119 行）后添加：

```javascript
  socket.on('foldToSpectate', ({ roomId }) => {
    const room = roomManager.getRoom(roomId);
    if (!room) return;
    const player = room.players.find(p => p.socketId === socket.id);
    if (!player || !player.folded || player.status === 'spectating') return;
    player.status = 'spectating';
    player.readyStatus = 'spectating';
    broadcastToEach(io, room, 'gameStateUpdate');
  });
```

### 7b：前端站起/坐下按钮

- [ ] **Step 2：在行动区 else 分支添加两个按钮**

找到行动区 else 分支内（约第 592 行），在「观战提示」div 之后、筹码信息 div 之前插入：

```jsx
              {/* 弃牌后可随时站起观战 */}
              {me?.folded && me?.status !== 'spectating' &&
               ['preflop','flop','turn','river'].includes(room.phase) && (
                <button
                  onClick={() => socket.emit('foldToSpectate', { roomId })}
                  style={{
                    background: 'rgba(255,255,255,0.07)', border: '1px solid rgba(255,255,255,0.18)',
                    borderRadius: 12, color: 'rgba(255,255,255,0.75)', fontWeight: 600,
                    fontSize: 13, padding: '8px 22px', cursor: 'pointer',
                  }}
                >👁 站起观战</button>
              )}

              {/* 观战中可随时坐下（下局参与） */}
              {me?.status === 'spectating' &&
               ['preflop','flop','turn','river'].includes(room.phase) &&
               me?.readyStatus !== 'queued' && (
                <button
                  onClick={sendQueueNextHand}
                  style={{
                    background: 'rgba(240,208,96,0.12)', border: '1px solid rgba(240,208,96,0.35)',
                    borderRadius: 12, color: '#f0d060', fontWeight: 600,
                    fontSize: 13, padding: '8px 22px', cursor: 'pointer',
                  }}
                >🪑 坐下（下局参与）</button>
              )}
              {me?.status === 'spectating' && me?.readyStatus === 'queued' &&
               ['preflop','flop','turn','river'].includes(room.phase) && (
                <div style={{ color: '#fbbf24', fontSize: 13, fontWeight: 600 }}>🟡 下局将参与</div>
              )}
```

- [ ] **Step 3：提交**

```bash
git add server/sockets/socketHandlers.js src/pages/GameRoom.jsx
git commit -m "feat: fold-to-spectate anytime + sit-down button during game"
```

---

## 自检

**Spec 覆盖：**
- [x] Task 1 → 动态座位（第5条）
- [x] Task 2 → 加注 UI（第1条）
- [x] Task 3 → 按钮立体感 + 音效（第2条部分）
- [x] Task 4 → 行动徽章（第2条完整）
- [x] Task 5 → 胜者动画 + 结算 3s 简化（第3条 + 结算简化）
- [x] Task 6 → 公共牌 + 亮牌显示牌型（第4条）
- [x] Task 7 → 弃牌随时观战 + 坐下（第6条）
- [x] Spec 第7条（中途加入默认观战）已在上一批实现，不在本计划范围

**类型一致性：**
- `playActionSound(action)` 在 Task 3 定义，Task 4b 使用 ✅
- `broadcastToEach(io, room, event, extra)` 在 Task 4a Step 1 定义，Task 4a Step 2/3/4 使用 ✅
- `processAfterAction(room, roomId, lastAction)` 在 Task 4a Step 2 定义，Step 3/4 使用 ✅
- `WinAnimation({ results })` 在 Task 5 Step 4 定义，Step 5 使用 ✅
- `ActionBadge({ badge })` 在 Task 4b Step 7 定义，Step 6 使用 ✅
- `SettlementScreen` 新签名 `{ settlementData, room, mySocketId, settlementCountdown, cardReveals, onRevealCards }` 在 Task 6 Step 1 定义，Task 5 Step 5 调用处匹配 ✅
